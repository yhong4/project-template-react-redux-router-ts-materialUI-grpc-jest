import { combineReducers } from 'redux';
import { AppStateReducer } from '../containers/Layout/reducer';

const reducers = combineReducers(
    {
        AppStateReducer
    }
)

export default reducers;
